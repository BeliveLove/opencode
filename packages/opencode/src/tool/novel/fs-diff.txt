Show a diff for a file inside the current novel directory using git.

- `aRef` and `bRef` are git refs (e.g. HEAD, HEAD~1, a commit hash).
- You can use the special ref `WORKTREE` to compare against the current working tree.
