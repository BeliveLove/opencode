Update skill(s) using the CLI command below.
If no arguments were provided or the command fails, explain the error and ask for the missing inputs.

!`opencode skill update $ARGUMENTS`

After running, summarize what was updated and where.
Default global location is ~/.config/opencode/skill/<name>/SKILL.md.
