先调用 `skill` 工具加载 `novel-plan`，并严格按技能契约输出。

目标章节（必须是 CH_*）：

$ARGUMENTS

执行步骤：

1) 读取 `config.yml`（风格/禁忌/POV/题材）与 `canon/` 相关文件作为上下文（用 `novel_fs_read`）。
2) 按技能要求输出结构化 `chapter_outline` 与 `scene_cards`（YAML）。
3) 将结果写入 `outlines/<CH>.outline.md`（用 `novel_fs_write`，`mode=overwrite`，必须落盘，不能只输出到对话里）。

约束：

- 场景卡必须使用 `SC_<章节ID>_<序号>`。
- 必须明确列出每个场景的信息投放（refs：RULE_/FB_/CHAR_/ORG_/LOC_/ITEM_）。
